namespace InputExample
{
    // Set up an enum to use for the six different animations.
    enum Animation
    {
      WalkUp,
      WalkDown,
      WalkLeft,
      WalkRight,
      PlayOnce,
      PingPong
    }
}
